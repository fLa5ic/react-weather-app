import React from 'react';

import styles from './NotFoundBlock.module.scss';

const NotFoundBlock = () => {
  return <div className={styles.title}>No search result found!</div>;
};

export default NotFoundBlock;
